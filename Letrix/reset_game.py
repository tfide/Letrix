import pygame
import random
from settings import *


"""
def reset_game(temas):
    nome_tema_sorteado = random.choice(list(temas.keys()))
    palavras = temas[nome_tema_sorteado]
    palavra_sorteada = random.choice(palavras)
    palavra_lista = list(palavra_sorteada)
    print(nome_tema_sorteado)
    print(palavra_sorteada)
    tentativas = 0
    y_offsets = [0]
    colors = [[branco] * 5]
    input_texts = ['']
    return tentativas, y_offsets, colors, input_texts, palavra_sorteada, palavra_lista
"""


